# Lessons

Patterns promoted to rules. A pattern needs to fire twice — once is a fluke, twice is a tendency, three times is a rule.

## Schema

```
## <one-line rule>
- Why: <the failure mode this rule prevents>
- How to apply: <when this rule kicks in>
- Evidence: <links to commits / sessions / handoffs that surfaced it>
```

## Seed entries

(The template ships empty. Add entries as your project teaches you things.)

<!-- mir:generated:start -->
## Active Lessons (DB projection)

_None recorded yet. Lessons inserted via `uv run mir memory insert --predicate lesson ...` are rendered here by `uv run mir memory render --target lessons --apply --output-path tasks/lessons.md`._
<!-- mir:generated:end -->
