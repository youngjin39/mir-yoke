# Change log

One bullet per non-trivial change. Newest at the top.

- YYYY-MM-DD: ran `./setup.sh` to install the harness; verified the pre-tool-use hook denies `rm -rf /` and the post-edit-check hook flags a sample `console.log`.
