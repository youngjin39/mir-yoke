# Checklist

- [x] Reconcile ADR-78, MODE-S requirements, implementation, and protected lock provenance.
- [x] Pass clean-candidate tests, lint, derivatives, sanitization, schemas, links, and consistency.
- [x] Classify every tracked release candidate exactly once and reject prohibited active paths.
- [x] Curate the canonical handoff, intent, plan, and verification ledger without a duplicate
  summary.
- [x] Confirm local and remote Git expose only `main` and that `origin/main` matches the published
  implementation state.
- [x] Restore the optional public v0.8 `mir` CLI and installed-package regression without restoring
  the active ADR-82 `yoke` composer.
- [x] Make the standard Project Agent Kit require bounded common harness and SQLite+FTS5 memory while
  keeping the Minimal Starter at four files and excluding copied provider runtime source.
- [x] Preserve the 33-file ADR-82 composition implementation under inert reference templates.
- [x] Create the implementation and capability-lock commits, push `main` and `v0.9.0` over SSH, and
  publish the verified GitHub Release.
