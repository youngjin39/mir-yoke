# Local Collaboration Pack

Delegation is optional and proportional. The active repository session owns final scope and
verification. Use isolated worktrees only when parallel mutation or rollback value exceeds their
coordination cost.

Runtime adapters map the neutral roles in `config/agent-roles.json` to locally available tools.
No adapter may redefine a shared role as belonging to a different runtime.
