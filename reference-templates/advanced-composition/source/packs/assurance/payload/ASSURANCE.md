# Local Assurance Pack

Use assurance artifacts proportionally. A bounded task does not need a full specification lattice.
When this repository selects a requirement, record its implementation anchor and the smallest
verification that can fail. Missing required evidence is not a pass.
