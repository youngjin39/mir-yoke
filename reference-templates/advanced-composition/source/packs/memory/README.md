# Memory Pack

This preview pack preserves the existing SQLite/FTS engine as source material while making its
consumer state boundary explicit. A project adopts the guidance and configuration first, then
integrates only the required engine surfaces.

Ordinary edit ingestion is advisory and incremental. Session closeout and release checks may
select strict verification when the repository contract requires it.
