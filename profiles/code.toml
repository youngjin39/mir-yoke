schema_version = 1
name = "code"
description = "Minimal core with deterministic safety defaults for code repositories."

[composition]
default_packs = ["safety"]
recommended_packs = ["collaboration", "assurance"]

[policy]
mandatory = false
