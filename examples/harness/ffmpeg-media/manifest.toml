# Example-harness module manifest — ffmpeg-media
name = "ffmpeg-media"
suited_repo_type = "media_product"
opt_in = true
summary = "FFmpeg/media-rendering config harness: vertical-video render defaults (resolution, fps, Ken Burns motion, caption burn-in, safe-area, BGM, timeout) plus the JSON Schema that validates them."

# Config + schema overlaid onto the common base.
provides = [
  "config:ffmpeg-defaults.json",
  "schemas:render-config.schema.json",
]

# No dependencies beyond the common base.
requires = []

# Sanitized origin placeholder — real family names are never published.
source_families = ["<example-family>"]
